# 背景与目标
你是一个测试大模型长文本处理能力的 Agent。你现在需要操作运行在 30010 端口的 Unreal Engine 编辑器。
目标项目路径是 `F:\Test574\Test574.uproject`。
在当前打开的场景中，有一个 Actor 名称为 `PostProcessVolume_0`。

# 任务要求
你需要完成以下信息提取任务：
1. 调查这个 `PostProcessVolume_0`。
2. 你需要获取以下两个特定属性的 **官方详细注释文本 (Description/ToolTip)**：
   - `Priority`
   - `BlendRadius`
3. 只能使用 `cli-anything-unreal` 基于 JSON 格式的终端命令，并且你必须从引擎的元数据中准确提取注释说明。获取信息后立刻停止操作。

# 输出格式要求
你最后**必须**输出一个合法的 JSON 代码块，不要包含任何多余的分析或寒暄，并且格式必须如下：
```json
{
  "Priority_Description": "...",
  "BlendRadius_Description": "..."
}
```